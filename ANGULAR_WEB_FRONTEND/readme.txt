This is web interface developed using Angular.
Make sure that latest angular version is installed in your system.
Run : npm install (to install all the node packages)
Then : npm serve --open (to run the project)

The project is hosted on : www.plax.tech