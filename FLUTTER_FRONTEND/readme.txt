This is mobile application developed using Flutter.

Make sure to set-up latest versions of flutter and dart.

Run  : flutter pub get (to get all the dependencies installed in your local environment)
Run  : flutter build apk (to build the solution)
Then : flutter run apk (to run the solution)
 